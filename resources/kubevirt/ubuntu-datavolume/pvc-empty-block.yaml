apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ubuntu-vm-data
spec:
  volumeMode: Block
  storageClassName: hpe-standard01
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 16Gi
