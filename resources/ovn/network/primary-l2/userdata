#cloud-config
ssh_pwauth: true

users:
  - name: rocky
    shell: /bin/bash
    groups: [wheel, sudo, users]
    sudo: ['ALL=(ALL) NOPASSWD:ALL']
    lock_passwd: false

chpasswd:
  list: |
    rocky:hogehoge
  expire: False
